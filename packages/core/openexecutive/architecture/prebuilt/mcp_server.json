{
  "section_id": "mcp_server",
  "title": "MCP Server",
  "markdown": "Open Executive can run **as an MCP server**, the inverse of the MCP *gateway* (which lets the Executive consume external MCP servers). The server exposes Open Executive's company-grounded context and its specialist council to external MCP clients — Claude Desktop, **Cursor CLI**, **OpenCode**, Claude Code, or any other agent — so they can ground themselves in *this* company and pull domain analysis **inside their own loop**, without re-explaining anything.\n\nThis is deliberately *not* another chat channel: Open Executive already has six of those (web, Slack, Discord, Telegram, email, Google Chat). The value here is structured, attachable context plus on-demand specialist analysis.\n\n## Transport & mount\n\nThe server speaks **Streamable-HTTP** and is mounted into the existing FastAPI app at `/mcp` (built with the `FastMCP` SDK, `streamable_http_path=\"/\"` mounted under `/mcp` so the endpoint is exactly `/mcp`). Mounting a sub-app does not run its lifespan, so the API lifespan runs `mcp.session_manager.run()` for the life of the process — without it every `/mcp` request would fail. The module lives at `openexecutive/mcp_server/` and is wired in `api/main.py`.\n\n## Auth\n\nThe `/mcp` endpoint is **not** exempt from the shared-secret middleware, so it is gated exactly like every other route. Clients may send either:\n\n- `x-api-key: $BACKEND_SHARED_SECRET` (same as the UI REST proxy), or\n- `Authorization: Bearer $BACKEND_SHARED_SECRET` — a **Bearer alias of the same secret**, preferred in Cursor and OpenCode MCP configs (see `examples/clients/cursor/mcp.json.example` and `examples/clients/opencode/`).\n\nThere is no second auth system. FastMCP's DNS-rebinding (Host-header) check is disabled because the endpoint authenticates with a header secret rather than a cookie — a browser rebinding attack cannot supply the key — and traffic arrives through the deployment's TLS terminator.\n\n## Resources (read-only context)\n\nThe highest-value surface: curated company state, pure reads, no LLM calls.\n\n| Resource URI | Contents |\n|---|---|\n| `oe://company/profile` | Structured company profile, as a prompt block |\n| `oe://today/briefing` | Morning-brief snapshot: department health, roster, proposals |\n| `oe://today/activity` | Recent self-initiated Executive activity |\n| `oe://people/roster` | Active people on the roster |\n| `oe://departments/state` | Departments with goals and cadences |\n| `oe://memory/decisions` · `…/initiatives` · `…/advice` | Episodic memory |\n\n## Tools (capabilities)\n\n| Tool | What it does |\n|---|---|\n| `consult_specialist(specialist, query, context)` | **Flagship.** Domain analysis from one of nine specialists (cso, cfo, chro, gc, coo, cmo, cpo, board_comms, triage), each grounded in company knowledge. Stateless. |\n| `search_knowledge(query)` | Raw retrieval over the MBA knowledge base + indexed company docs |\n| `list_workflows()` | Catalog of multi-step executive workflows and their input schemas |\n| `ask_executive(message, caller_email?)` | A single synthesized Executive answer — a fallback; prefer the specialist tool and resources |\n\n\n## What's intentionally deferred\n\n**Workflow *execution*** is not exposed over MCP yet. Workflows are long-running async generators (minutes), which would exceed a single tool call's request/response window. The catalog is listed via `list_workflows()`; if execution is added later it will use a job-handle pattern (`start_workflow` → `run_id`, then poll) over the existing run persistence. MCP *prompts* are also skipped — OE workflows are server-executed orchestrations, not message templates.\n\n## Inverse: outbound coding agents\n\nWhen OE needs hands *inside a repo* (ask/plan against an allowlisted workspace), that is the **Coding Agents** architecture section — Executive tools dispatch Cursor/OpenCode jobs; inbound MCP is the opposite direction.\n\n## Why prompt caching is unaffected\n\nThis path is parallel to chat. Resources are pure reads; `consult_specialist` delegates to the same `BaseAgent.analyze` that the chat path uses, reusing the existing cached system blocks. Nothing here touches the cache layout.",
  "mermaid": "flowchart TD\n    Client[\"External MCP client<br/>(Claude Desktop / Cursor CLI / OpenCode / agent)\"]:::entry\n    Gate[\"Shared-secret middleware<br/>x-api-key or Authorization Bearer\"]:::entry\n    Mount[\"/mcp Streamable-HTTP<br/>(FastMCP, mounted in FastAPI)\"]:::compute\n    Res[\"Resources<br/>profile · today · people<br/>departments · memory\"]:::compute\n    Tools[\"Tools<br/>consult_specialist · search_knowledge<br/>list_workflows · ask_executive\"]:::compute\n    Spec[\"Specialist council<br/>route_to_specialist → BaseAgent.analyze\"]:::compute\n    Store[(\"ChromaDB + SQLite<br/>company state\")]:::storage\n\n    Client -->|Bearer or x-api-key| Gate --> Mount\n    Mount --> Res\n    Mount --> Tools\n    Res --> Store\n    Tools --> Spec\n    Tools --> Store\n    Spec --> Store\n\n    classDef entry fill:#1e3a8a,stroke:#60a5fa,color:#fff;\n    classDef compute fill:#312e81,stroke:#a5b4fc,color:#fff;\n    classDef storage fill:#365314,stroke:#a3e635,color:#fff;",
  "generated_at": "2026-09-20T20:00:00Z"
}
